apiVersion: v1
kind: Secret

metadata:
    name: conference-secret

type: Opaque

# Local development credentials only — replace before any shared cluster.
stringData:
    PG_USER: admin
    PG_PASSWORD: admin
    DRAGONFLY_PASSWORD: ""